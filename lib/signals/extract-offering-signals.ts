import { OfferingSignal } from './offering-signal';

export async function extractOfferingSignals(entityId: string): Promise<OfferingSignal[]> {
  // Placeholder for future pipeline
  // Menu scan → wine list → editorial → merchant text

  return [];
}
