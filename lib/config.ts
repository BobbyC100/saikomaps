/**
 * DEV: Show all auth-gated UI regardless of session/ownership.
 * Set to false before launch.
 * Does NOT affect API routes - they still require real auth.
 */
export const DEV_SHOW_ALL_UI = true;
